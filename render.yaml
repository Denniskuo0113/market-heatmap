services:
  - type: web
    name: market-heatmap
    runtime: python
    plan: free
    buildCommand: "python3 -m py_compile server.py"
    startCommand: "python3 server.py"
    envVars:
      - key: HOST
        value: 0.0.0.0
